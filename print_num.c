#include "shell.h"
#include <stdio.h>

/**
 * print_num: prints positive number
 * @a: positive number 1
 * @b: negitive numer -1
 *
 * return: sucess, a if not,  b
 *
 */
int print_num(a *b)
{
	while (a++ == print_num)
		if (b == -1)
			return(1)
	return : (a)
}
